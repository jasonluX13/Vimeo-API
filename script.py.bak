from flask import Flask,request,url_for,redirect,render_template
import lib2to3
import json, urllib2 #urllib.request, urllib.error

app=Flask(__name__)
@app.route("/")
def index():
    return "hello"
@app.route("/t/<tag>")
def tag(tag="narwhal"):
    url="http://gateway.marvel.com/v1/comics/?ts=1&apikey=c6202d64bfb8be20e9a7986bda8963bc&hash=fe624c998b67c8998326e513e73a34da"
    #url = url%(tag)
    request = urllib2.urlopen(url)
    resultstring = request.read()
    result = json.loads(resultstring)
    s = ""
    for item in result['response']:
        print( item)
    try:
        s= s + "<img src=%s>"%(item['photos'][0]['original_size']['url'])
        print (s)
    except:
        pass
    return s
if __name__=="__main__":
    app.debug=True
    app.run(host="0.0.0.0",port=8000)